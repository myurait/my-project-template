# Feature Backlog

This file is the structured deferred request ledger. Use it to keep non-roadmap items rich enough that later planning does not underestimate them.

## Status

- Purpose: keep deferred requests as structured planning inputs outside the active roadmap
- Rule: items here are deferred requests, not implementation-ready by default
- Rule: only `near` items are candidates for promotion to `features/supporting/`
- Rule: `heavy` or `architectural` items must have their constraints reflected in the active design docs before adjacent implementation proceeds
