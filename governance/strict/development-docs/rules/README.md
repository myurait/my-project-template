# Rules

This directory stores project rules that are treated as stable by default.

- Files under `rules/` must not be changed without explicit user direction or a clearly authorized rule-maintenance task.
- Day-to-day implementation work should follow these documents, not casually edit them.
- When a task reveals tension with an existing rule, record the issue in normal project documents or reviews first, then change the rule only with explicit approval.
